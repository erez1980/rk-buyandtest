# אבטחה

## דיווח על פגיעוּת

פתחו issue בריפו. אין כאן תוכנית באג באונטי.

## מודל האיום

הפרונטאנד הוא קובץ סטטי שכל אחד יכול לקרוא. **כל מה שנמצא בו הוא פומבי**,
כולל מפתח ה־`anon` של Supabase — וכך הוא מתוכנן. ההגנה על נתוני הלקוחות
נשענת על שתי שכבות:

1. **RLS מופעל על כל הטבלאות, ללא מדיניות המתירה ל־`anon`.** התוצאה היא
   חסימה מלאה: `anon` לא יכול לקרוא או לכתוב לאף טבלה, ולא להריץ את
   פונקציות הקונפיגורציה או מימוש ההרשאות.
2. **כל גישה לנתונים עוברת דרך Edge Functions** שרצות כ־`service_role`,
   בודקות `origin` מול סוד, ומאמתות הרשאת תשלום חתומה ב־HMAC.

מה שאסור שיגיע לקוד הפומבי: מפתח `service_role`, מפתח Google Vision,
פרטי Cardcom, וסוד ה־HMAC. כולם יושבים בסודות הפרויקט או ב־`buytest_private_config`.

## סודות שדלפו

| מפתח | סוג | ב־HEAD | סטטוס |
|---|---|---|---|
| `AIzaSyDWVj…` | Google Vision API | לא | **ממתין לביטול** |
| `AIzaSyC7sY…` | Google Vision API | לא | **ממתין לביטול** |

שני המפתחות הוסרו מהקוד הנוכחי, אך **נשארים בהיסטוריה של ריפו ציבורי**
וניתנים לשליפה על ידי כל אחד. מחיקה מהקוד אינה ביטול. עד שיבוטלו
ב־Google Cloud Console אפשר לחייב באמצעותם את חשבון ה־GCP של הבעלים.

הרשימה המלאה: `security/exposed-keys.json`. היא שומרת SHA-256 בלבד, כדי
שהתיעוד עצמו לא יפרסם את המפתחות מחדש.

**סדר הפעולות הנכון:** לבטל אצל הספק ← לתעד כאן ← ורק אז, אם בכלל, לשקול
שכתוב היסטוריה. שכתוב היסטוריה בלי ביטול נותן תחושת ביטחון מוטעית: העתקים
של הריפו, forks ומטמוני GitHub ממשיכים להחזיק את הקומיטים הישנים.

## אכיפה אוטומטית

| בדיקה | מה היא תופסת |
|---|---|
| `tests/static_checks.py` | סוד בעץ העבודה, מפתח `service_role` בקוד הפומבי |
| `tests/history_scan.py` | סוד **בכל היסטוריית git** שאינו מתועד ברשימה |

שתיהן רצות ב־CI וחוסמות. הוספת מפתח חדש ל־`exposed-keys.json` כדי לעקוף
כישלון היא לא תיקון — הרשימה מיועדת לסודות שכבר בוטלו או שממתינים לביטול,
ולא לעקיפת הבדיקה.
